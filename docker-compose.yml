version: "3"

services:
  db:
    image: postgres:11
    environment:
      POSTGRES_USER: post
      POSTGRES_DB: post
      POSTGRES_PASSWORD: post
    volumes:
      - dbdata:/var/lib/postgresql/data

  backend:
    build: backend
    volumes:
      - ./backend:/usr/src/app
    ports:
      - 9001:9000
    environment:
      DB_URL: ${DB_URL}
    depends_on:
      - db

  frontend:
    build: frontend
    volumes:
      - ./frontend:/usr/src/app
    ports:
      - 3001:3000

volumes:
  dbdata:
